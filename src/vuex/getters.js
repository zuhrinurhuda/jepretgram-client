const getters = {
  users: state => state.users,
  userProfile: state => state.userProfile,
  photos: state => state.photos,
  userPhotos: state => state.userPhotos,
  photoDetail: state => state.photoDetail
}

export default getters
